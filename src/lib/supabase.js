// Este archivo ya no se usa. El frontend se comunica con el backend
// a través de src/lib/api.js (Axios con proxy /api → backend Express).
// La conexión a Supabase la maneja exclusivamente el backend.
//
// Si ves este import en algún componente, reemplázalo con:
//   import api from './api'

export const supabase = null
